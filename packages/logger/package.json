{
  "name": "@monorepo/logger",
  "version": "0.1.0",
  "description": "Extensible structured logging utility for the monorepo.",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "lint": "bun x ultracite lint",
    "format": "bun x ultracite format"
  },
  "dependencies": {
    "pino": "^9.3.1",
    "pino-multi-stream": "^6.0.0",
    "pino-rotating-file": "^3.5.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
